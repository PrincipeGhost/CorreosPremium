services:
  - type: web
    name: enviospro-web
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # DATABASE_URL debe ser añadida manualmente en Render Dashboard
      # Usa tu Connection String de Neon
